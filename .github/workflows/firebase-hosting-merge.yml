# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm ci --omit=dev && npm run build
      - run: |
          export GOOGLE_APPLICATION_CREDENTIALS="$(mktemp -u).json"
          echo "${GOOGLE_APPLICATION_CREDENTIALS}"
          echo "${{ secrets.FIREBASE_SERVICE_ACCOUNT_SHARE_TIMER_2B51A }}" > "${GOOGLE_APPLICATION_CREDENTIALS}"
          npm run deploy:rules
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_SHARE_TIMER_2B51A }}"
          channelId: live
          projectId: share-timer-2b51a
